services:

  db:
    image: mariadb:11
    container_name: witty-accounting-db

    restart: unless-stopped

    environment:
      MYSQL_DATABASE: witty_accounting
      MYSQL_USER: witty
      MYSQL_PASSWORD: changeme
      MYSQL_ROOT_PASSWORD: rootpassword

    ports:
      - "3306:3306"

    volumes:
      - mariadb_data:/var/lib/mysql

  backend:
    build:
      context: ./backend

    extra_hosts:
      - "host.docker.internal=172.18.0.1"

    container_name: witty-accounting-backend

    restart: unless-stopped

    env_file:
      - .env

    depends_on:
      - db

    ports:
      - "8000:8000"

    volumes:
      - ./uploads/logs:/app/logs
      - ./uploads/invoices:/app/data/invoices

    secrets:
      - source: invoice_mail_signing_pkcs12
        target: invoice-mail-signing.p12
      - source: invoice_mail_signing_password
        target: invoice-mail-signing-password

secrets:
  invoice_mail_signing_pkcs12:
    file: ./secrets/invoice-mail-signing.p12

  invoice_mail_signing_password:
    file: ./secrets/invoice-mail-signing-password

volumes:
  mariadb_data:
